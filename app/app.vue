<script setup lang="ts">
const colorMode = useColorMode();

const color = computed(() => colorMode.value === "dark" ? "#1b1718" : "white");

useHead({
  meta: [
    { charset: "utf-8" },
    { name: "viewport", content: "width=device-width, initial-scale=1" },
    { key: "theme-color", name: "theme-color", content: color },
  ],
  link: [
    { rel: "icon", href: "favicon.ico" },
  ],
  htmlAttrs: {
    lang: "en",
  },
});

const title = "Kaduna Electric Asset Registry";
const description = "A platform for managing and tracking assets for Kaduna Electric, providing efficient asset registry and oversight.";

useSeoMeta({
  title,
  description,
});
</script>

<template>
  <UApp>
    <NuxtRouteAnnouncer />
    <NuxtLayout>
      <NuxtPage />
    </NuxtLayout>
  </UApp>
</template>
